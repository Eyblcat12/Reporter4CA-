# Applications

- `backend/` contains the FastAPI service, report engine, templates, and runtime data.
- `frontend/` contains the React/Vite user interface.

Keeping deployable applications together makes the repository easier to navigate
while shared automation and tests remain at the repository root.
