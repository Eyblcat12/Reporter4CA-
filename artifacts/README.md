# Local build artifacts

This directory holds generated desktop distributions used for local delivery and
manual verification. Binary output under `desktop/dist/` is intentionally ignored
by Git and is not part of the source tree.

Long-running and smoke-test results are written under `soak/`. They are local
diagnostic outputs and are also intentionally ignored by Git.
